import React from 'react';
import { connect } from 'react-redux';

export const RARC = props => {
  return <div>RARC</div>;
};

const mapStateToProps = state => ({});

const mapDispatchToProps = {};

export default connect(mapStateToProps, mapDispatchToProps)(RARC);
